# Issue tracker data (local, per-checkout)
.issues/

# Python
__pycache__/
*.py[cod]
*.egg-info/
.venv/
venv/
.eggs/
build/
dist/

# Node / frontend build
node_modules/
frontend/dist/
# NOTE: src/issue_tracker/static/ (the built SPA) is intentionally committed so
# that `git pull` delivers the current web UI without requiring a `npm run
# build`. Rebuild it with `cd frontend && npm run build` after changing the
# frontend.

# Editor / OS
.DS_Store
.idea/
.vscode/
